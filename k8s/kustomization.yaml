apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bridge

resources:
  - namespace.yaml
  - configmap.yaml
  - sealed-secret.yaml  # Sprint 85: Sealed Secrets (secret.yaml deprecated)
  - postgres.yaml
  - redis.yaml
  - bridge.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml
  - servicemonitor.yaml  # Sprint 85 fix: Prometheus ServiceMonitor
